<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ljn.xiaoruireading">

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name=".view.concrete_views.FirstWelcomActivity">

        </activity>
        <activity android:name=".view.concrete_views.HomeActivity">

        </activity>
        <activity android:name=".view.concrete_views.LoginActivity">

        </activity>
        <activity android:name=".view.concrete_views.RegActivity">

        </activity>
        <activity android:name=".view.concrete_views.ReaderActivity">

        </activity>
        <activity android:name=".view.concrete_views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

    <supports-screens
            android:smallScreens="true"
            android:normalScreens="true"
            android:largeScreens="true"
            android:resizeable="true"
            android:anyDensity="true" />

</manifest>